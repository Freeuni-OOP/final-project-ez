/**
 * REST controllers — the HTTP entry points. Controllers stay thin: they parse the
 * request, delegate to a service, and return a response. No business logic here.
 */
package com.algorythm.controller;
